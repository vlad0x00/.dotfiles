status is-interactive; or return

alias mv 'mv -i'
alias cp 'cp -i'
alias rm 'rm -i'

alias gst 'git status'
alias gp  'git push'
alias gcb 'git checkout -b'
alias gl  'git pull'
alias gd  'git diff'
alias gc  'git commit'
alias gaa 'git add --all'
